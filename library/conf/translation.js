
var configuration = require("./configuration.js");

/**
 * Manages available session translation functions and makes them available
 * to the Library for loading sessions from various sources.
 * 
 * Additional translation functions can be added as node modules for customization
 * and subsequently configured in the security section of the instance configuration
 * or in the global system configuration to be inherited by libraries running on the
 * same host instance.
 * @static
 * @class Translation
 * @constructor
 */
module.exports = (function() {
	
})();
